# Как поделиться сайтом

Есть несколько способов показать сайт другому человеку:

## 🚀 Вариант 1: Vercel (Рекомендуется - самый простой)

### Через Vercel CLI (без Git):

1. **Установите Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

2. **Войдите в Vercel:**
   ```bash
   vercel login
   ```

3. **Задеплойте проект:**
   ```bash
   vercel
   ```
   
   Следуйте инструкциям:
   - Нажмите Enter для подтверждения настроек
   - Выберите "Link to existing project" или создайте новый
   - После деплоя вы получите URL вида: `https://minsk-cargo-landing.vercel.app`

4. **Поделитесь ссылкой!** Сайт будет доступен на любом устройстве.

### Через веб-интерфейс Vercel:

1. Зайдите на [vercel.com](https://vercel.com) и зарегистрируйтесь
2. Нажмите "Add New Project"
3. Подключите GitHub/GitLab (или используйте CLI как выше)
4. Импортируйте проект
5. Vercel автоматически задеплоит сайт

---

## 🌐 Вариант 2: Netlify (Альтернатива)

1. **Установите Netlify CLI:**
   ```bash
   npm install -g netlify-cli
   ```

2. **Соберите проект:**
   ```bash
   npm run build
   ```

3. **Задеплойте:**
   ```bash
   netlify deploy --prod
   ```

4. Следуйте инструкциям и получите URL

---

## 💻 Вариант 3: Локальный доступ через ngrok (для быстрого теста)

1. **Установите ngrok:**
   - Скачайте с [ngrok.com](https://ngrok.com)
   - Или через npm: `npm install -g ngrok`

2. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

3. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 3000
   ```

4. **Скопируйте URL** (например: `https://abc123.ngrok.io`) и поделитесь им

⚠️ **Внимание:** Сайт будет работать только пока запущен dev-сервер!

---

## 📦 Вариант 4: Через Git (если установите Git)

1. **Инициализируйте Git:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **Создайте репозиторий на GitHub:**
   - Зайдите на github.com
   - Создайте новый репозиторий
   - Следуйте инструкциям для push

3. **Подключите к Vercel:**
   - Зайдите на vercel.com
   - Импортируйте проект из GitHub
   - Vercel автоматически задеплоит

---

## ✅ Рекомендация

**Используйте Вариант 1 (Vercel CLI)** - это самый быстрый способ:
- Бесплатно
- Автоматический HTTPS
- Работает на любом устройстве
- Постоянный URL
- Автоматические обновления при изменениях

После деплоя вы получите URL вида:
`https://minsk-cargo-landing-xxx.vercel.app`

Поделитесь этим URL - сайт будет доступен на любом устройстве! 📱💻

