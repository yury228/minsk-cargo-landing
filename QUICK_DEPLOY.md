# 🚀 Быстрый деплой на Vercel

Vercel CLI уже установлен! Выполните следующие команды:

## Шаги:

1. **Войдите в Vercel** (откроется браузер для авторизации):
   ```
   vercel login
   ```

2. **Задеплойте проект**:
   ```
   vercel
   ```
   
   При первом запуске:
   - Нажмите `Enter` для подтверждения настроек
   - Выберите `Y` для создания нового проекта
   - Выберите `Y` для линковки к существующему проекту (если есть) или создайте новый
   - После деплоя вы получите URL!

3. **Для production деплоя** (опционально):
   ```
   vercel --prod
   ```

## Результат:

После выполнения команд вы получите URL вида:
```
https://minsk-cargo-landing-xxxxx.vercel.app
```

**Поделитесь этим URL** - сайт будет доступен на любом устройстве! 📱💻

---

## Альтернатива: Через веб-интерфейс

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Загрузите папку проекта через drag & drop
4. Или подключите GitHub/GitLab репозиторий

